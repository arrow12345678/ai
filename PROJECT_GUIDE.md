# 🚗 دليل مشروع التعرف على لوحات السيارات

## 🎯 نظرة عامة على المشروع

هذا مشروع تطبيقي شامل يهدف إلى **تطبيق مفاهيم التعلم العميق عملياً** في مجال التعرف على لوحات السيارات.

### 📋 المراحل الأساسية:

1. ✅ **تحديد الفكرة**: التعرف الآلي على لوحات السيارات
2. 📊 **جلب وتجهيز قاعدة البيانات**: من مصادر مختلفة
3. 🏗️ **بناء نموذج تعلم عميق**: شبكة عصبية تطبيقية (CNN)
4. 💻 **كتابة الكود**: تطبيق شامل بلغة Python
5. 🚀 **تدريب النموذج**: على البيانات المحضرة
6. 📈 **تحليل النتائج**: تقييم شامل للأداء

---

## 🗂️ هيكل المشروع

```
ali_project1/
├── 📊 البيانات
│   ├── data_downloader.py          # تحميل البيانات
│   └── data/
│       ├── raw/                    # البيانات الخام
│       ├── train/ (80%)            # بيانات التدريب
│       ├── val/ (10%)              # بيانات التحقق
│       └── test/ (10%)             # بيانات الاختبار
│
├── 🤖 النماذج
│   ├── complete_alpr_project.py    # النموذج الأساسي
│   ├── train_and_evaluate.py      # التدريب والتقييم
│   └── models/                     # النماذج المدربة
│
├── 📈 النتائج
│   └── results/                    # التحليلات والتقارير
│
└── 🚀 التشغيل
    └── run_complete_project.py     # التشغيل الشامل
```

---

## 🚀 كيفية التشغيل

### الطريقة السريعة (مشروع كامل):
```bash
python run_complete_project.py --mode full
```

### التشغيل المرحلي:

#### 1. إعداد البيانات فقط:
```bash
python run_complete_project.py --mode data
```

#### 2. التدريب فقط:
```bash
python run_complete_project.py --mode train --epochs 30 --batch_size 32
```

#### 3. التقييم فقط:
```bash
python run_complete_project.py --mode eval
```

### التشغيل المنفصل:

#### إعداد البيانات:
```bash
python data_downloader.py
```

#### تشغيل المشروع الأساسي:
```bash
python complete_alpr_project.py
```

#### التدريب والتقييم:
```bash
python train_and_evaluate.py
```

---

## 📊 مصادر قواعد البيانات

### 1. 🎨 البيانات التجريبية (مدمجة)
- **الوصف**: عينات مُنشأة تلقائياً للتجربة
- **الحجم**: قابل للتخصيص (افتراضي: 200 عينة)
- **المزايا**: سريع، لا يحتاج إنترنت
- **الاستخدام**: للتجربة والتعلم

### 2. 🌐 Kaggle Datasets
- **الرابط**: https://www.kaggle.com/datasets/andrewmvd/car-plate-detection
- **المتطلبات**: حساب Kaggle + VPN
- **الحجم**: ~100MB
- **الجودة**: عالية

### 3. 📁 GitHub Repositories
- **مثال**: https://github.com/detectRecog/CCPD
- **الحجم**: ~500MB
- **المزايا**: مجاني ومفتوح المصدر

### 4. 🔍 OpenImages Dataset
- **الرابط**: https://storage.googleapis.com/openimages/web/index.html
- **الحجم**: ~2GB
- **المزايا**: شامل ومتنوع

---

## 🏗️ تفاصيل النموذج

### معمارية الشبكة العصبية:
```python
Input Layer: (128, 384, 3)
    ↓
Conv2D + BatchNorm + MaxPool + Dropout (32 filters)
    ↓
Conv2D + BatchNorm + MaxPool + Dropout (64 filters)
    ↓
Conv2D + BatchNorm + MaxPool + Dropout (128 filters)
    ↓
Conv2D + BatchNorm + MaxPool + Dropout (256 filters)
    ↓
GlobalAveragePooling2D
    ↓
Dense(512) + BatchNorm + Dropout
    ↓
Dense(256) + BatchNorm + Dropout
    ↓
Dense(36) [Output: A-Z + 0-9]
```

### المعاملات:
- **حجم الدخل**: 128×384×3 (ارتفاع × عرض × قنوات)
- **عدد الفئات**: 36 (26 حرف + 10 أرقام)
- **دالة التفعيل**: ReLU للطبقات المخفية، Softmax للخرج
- **المحسن**: Adam
- **دالة الخسارة**: Sparse Categorical Crossentropy

---

## 📈 تقسيم البيانات

### التوزيع المعياري:
- **🚀 التدريب (Training)**: 80% من البيانات
  - لتعليم النموذج الأنماط والميزات
- **✅ التحقق (Validation)**: 10% من البيانات
  - لمراقبة الأداء أثناء التدريب
- **🧪 الاختبار (Testing)**: 10% من البيانات
  - لتقييم الأداء النهائي

### سبب هذا التقسيم:
- **80% تدريب**: يوفر بيانات كافية لتعلم الأنماط
- **10% تحقق**: يمنع الإفراط في التدريب (Overfitting)
- **10% اختبار**: يعطي تقييم موضوعي للأداء

---

## 📊 النتائج والتحليلات

### الملفات المُنتجة:

#### 🤖 النماذج:
- `models/best_model.h5` - أفضل نموذج أثناء التدريب
- `models/final_model.h5` - النموذج النهائي

#### 📈 التحليلات:
- `results/complete_analysis.png` - رسوم بيانية شاملة
- `results/training_history.png` - منحنيات التدريب
- `results/evaluation_results.json` - نتائج مفصلة

#### 📄 التقارير:
- `results/project_report.md` - تقرير شامل
- `results/training_log.csv` - سجل التدريب

### مؤشرات الأداء:
- **الدقة (Accuracy)**: نسبة التنبؤات الصحيحة
- **الدقة (Precision)**: نسبة التنبؤات الإيجابية الصحيحة
- **الاستدعاء (Recall)**: نسبة الحالات الإيجابية المكتشفة
- **F1-Score**: المتوسط التوافقي للدقة والاستدعاء

---

## 🛠️ التخصيص والتطوير

### تخصيص المعاملات:
```python
# في run_complete_project.py
python run_complete_project.py --epochs 50 --batch_size 32

# في الكود مباشرة
model = ALPRModel(
    input_shape=(128, 384, 3),
    num_classes=36
)
```

### إضافة بيانات جديدة:
1. ضع الصور في `data/raw/`
2. أضف ملفات التسمية (.txt) بنفس الأسماء
3. شغل `python run_complete_project.py --mode data`

### تحسين النموذج:
- زيادة عدد العصور (epochs)
- تجربة أحجام دفعات مختلفة
- إضافة طبقات أو تعديل المعمارية
- استخدام تقنيات تعزيز البيانات

---

## 🔧 استكشاف الأخطاء

### مشاكل شائعة وحلولها:

#### 1. خطأ في استيراد TensorFlow:
```bash
pip install tensorflow>=2.13.0
```

#### 2. نفاد الذاكرة:
- قلل حجم الدفعة: `--batch_size 8`
- قلل حجم الصورة في الكود

#### 3. بطء التدريب:
- استخدم GPU إذا متوفر
- قلل عدد العصور للتجربة

#### 4. دقة منخفضة:
- زد عدد العصور
- أضف المزيد من البيانات
- جرب تقنيات تعزيز البيانات

---

## 📚 المفاهيم المطبقة

### تعلم الآلة:
- **الشبكات العصبية التطبيقية (CNN)**
- **التدريب المُشرف (Supervised Learning)**
- **تقسيم البيانات**
- **تقييم الأداء**

### الرؤية الحاسوبية:
- **معالجة الصور**
- **استخراج الميزات**
- **التعرف على الأنماط**

### هندسة البرمجيات:
- **البرمجة الكائنية**
- **إدارة المشاريع**
- **التوثيق**
- **اختبار الكود**

---

## 🎓 الأهداف التعليمية

بعد إكمال هذا المشروع، ستكون قادراً على:

1. ✅ **فهم دورة حياة مشروع التعلم العميق**
2. ✅ **تطبيق الشبكات العصبية التطبيقية عملياً**
3. ✅ **إدارة وتنظيم قواعد البيانات**
4. ✅ **تقييم وتحليل أداء النماذج**
5. ✅ **كتابة كود Python احترافي**
6. ✅ **إنشاء تقارير ومرئيات للنتائج**

---

## 🚀 الخطوات التالية

### للمبتدئين:
1. شغل المشروع بالإعدادات الافتراضية
2. اطلع على النتائج والتحليلات
3. جرب تعديل المعاملات البسيطة

### للمتقدمين:
1. أضف بيانات حقيقية من Kaggle
2. طور معمارية النموذج
3. طبق تقنيات متقدمة (Transfer Learning, Data Augmentation)
4. انشر النموذج كتطبيق ويب

---

## 📞 الدعم والمساعدة

إذا واجهت أي مشاكل:
1. تحقق من ملف `results/training_log.csv` للأخطاء
2. راجع الرسائل في Terminal
3. تأكد من تثبيت جميع المكتبات المطلوبة

---

🎉 **مبروك! أنت الآن جاهز لتطبيق مفاهيم التعلم العميق عملياً!**
